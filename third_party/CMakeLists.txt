if(BUILD_FLATCC)
    set(FLATCC_EXAMPLES OFF CACHE BOOL "" FORCE)
    set(FLATCC_INSTALL OFF CACHE BOOL "" FORCE)
    set(FLATCC_RTONLY OFF CACHE BOOL "" FORCE)
    set(FLATCC_TEST OFF CACHE BOOL "" FORCE)
    function(add_flatcc_static)
        set(BUILD_SHARED_LIBS OFF)
        set(CMAKE_POSITION_INDEPENDENT_CODE ON)
        add_subdirectory(flatcc)
    endfunction()
    add_flatcc_static()
else()
    message(STATUS "third_party: FlatCC disabled (BUILD_FLATCC=OFF)")
endif()

if(BUILD_TESTING)
    add_subdirectory(Unity)
else()
    message(STATUS "third_party: Unity disabled (BUILD_TESTING=OFF)")
endif()

if (BUILD_PYTHON)
    set(FLATBUFFERS_BUILD_TESTS OFF CACHE BOOL "" FORCE)
    set(FLATBUFFERS_INSTALL OFF CACHE BOOL "" FORCE)
    set(FLATBUFFERS_BUILD_FLATC ON CACHE BOOL "" FORCE)
    if(MINGW)
        set(FLATBUFFERS_STATIC_FLATC ON CACHE BOOL "" FORCE)
    endif()
    add_subdirectory(flatbuffers)
else()
    message(STATUS "third_party: FlatBuffers disabled (BUILD_PYTHON=OFF)")
endif()